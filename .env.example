# Python 腳本使用的環境變數
# 複製此檔案為 .env（已被 .gitignore 排除，不會上傳到 GitHub）
#
# 使用方式：
#   cp .env.example .env
#   填入你的實際 API Key 後，執行：
#   export $(cat .env | xargs) && python3 batch_process.py

# Chandra OCR 2（Datalab）API Key
# 取得網址：https://www.datalab.to/app/keys
DATALAB_API_KEY=your_datalab_api_key_here
